import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import { DocumentStatus } from "./DocumentStatus";
import {
  whenDocumentValidAndIssued,
  whenDocumentHashInvalid,
  whenDocumentNotIssued,
  whenDocumentRevoked,
  whenDocumentIssuerIdentityInvalid,
  whenDocumentHashInvalidAndNotIssued,
} from "../../test/fixture/verifier-responses";

<Meta title="DocumentStatus" component={DocumentStatus} />

# DocumentStatus

DocumentStatus look.

---

### Props

<Props of={DocumentStatus} />

---

### Document is Valid

Document is issued, has integrity, not revoked and has a verified issuer identity

<Preview>
  <Story name="Valid Document">
    {() => {
      return <DocumentStatus verificationStatus={whenDocumentValidAndIssued} />;
    }}
  </Story>
</Preview>

---

### Document Hash is invalid

Document was tampered with

<Preview>
  <Story name="Tampered Document">
    {() => {
      return <DocumentStatus verificationStatus={whenDocumentHashInvalid} />;
    }}
  </Story>
</Preview>

---

### Document not issued

Document is not issued

<Preview>
  <Story name="Not Issued Document">
    {() => {
      return <DocumentStatus verificationStatus={whenDocumentNotIssued} />;
    }}
  </Story>
</Preview>

---

### Document revoked

Document was issued and subsequently revoked

<Preview>
  <Story name="Revoked Document">
    {() => {
      return <DocumentStatus verificationStatus={whenDocumentRevoked} />;
    }}
  </Story>
</Preview>

---

### Document Issuer Identity invalid

Document's issuer identity cannot be verified

<Preview>
  <Story name="Invalid Issuer">
    {() => {
      return <DocumentStatus verificationStatus={whenDocumentIssuerIdentityInvalid} />;
    }}
  </Story>
</Preview>

---

### Document all failed

Document when all verification fail

<Preview>
  <Story name="All Failed">
    {() => {
      return <DocumentStatus verificationStatus={whenDocumentHashInvalidAndNotIssued} />;
    }}
  </Story>
</Preview>
