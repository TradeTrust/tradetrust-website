import { useState } from "react";
import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import { SelectDropdown, SelectDefault } from "./Select";

<Meta title="UI/Select" component={SelectDropdown} />

# Select

Select look.

---

### Props

<Props of={SelectDropdown} />

---

### Default

<Preview>
  <Story name="Default">
    {() => {
      const [selected, setSelected] = useState([]);
      const options = [
        { value: "A", label: "A" },
        { value: "B", label: "B" },
        { value: "C", label: "C" },
      ];
      return (
        <SelectDefault
          values={selected}
          options={options}
          onChange={setSelected}
          name="test"
          placeholder="Select some option"
        />
      );
    }}
  </Story>
</Preview>
