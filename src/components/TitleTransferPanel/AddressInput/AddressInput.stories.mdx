import { useState } from "react";
import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import { AddressInput } from "./AddressInput";
import { AddressButtons } from "./../AddressButtons";

<Meta title="TitleTransferPanel/AddressInput" component={AddressInput} />

# Address Input Single

Address Input Single.

---

### Props

<Props of={AddressInput} />

---

### Inlined

Buttons beside input block.

<Preview>
  <Story name="Inlined">
    {() => {
      const [address, setAddress] = useState("");
      return (
        <AddressInput address={address} setAddress={setAddress}>
          <AddressButtons buttonText="Transfer" onButtonSubmit={() => {}} holderAddress={address} />
        </AddressInput>
      );
    }}
  </Story>
</Preview>

---

### Error

Inlined error state.

<Preview>
  <Story name="InlinedErrored">
    {() => {
      const [address, setAddress] = useState("");
      return (
        <AddressInput address={address} setAddress={setAddress} errorMessage="There's an error.">
          <AddressButtons buttonText="Transfer" onButtonSubmit={() => {}} holderAddress={address} />
        </AddressInput>
      );
    }}
  </Story>
</Preview>

---

### Block

Buttons below input block.

<Preview>
  <Story name="Block">
    {() => {
      const [outgoingHolderAddress, setOutgoingHolderAddress] = useState("0x48399Fb88bcD031C556F53e9303CaF7aB32C9302");
      const [outgoingBeneficiaryAddress, setOutgoingBeneficiaryAddress] = useState(
        "0x28F7aB32C521D13F2E6980d072Ca7CA493020145"
      );
      return (
        <>
          <AddressInput address={outgoingHolderAddress} setAddress={setOutgoingHolderAddress} />
          <AddressInput address={outgoingBeneficiaryAddress} setAddress={setOutgoingBeneficiaryAddress} />
          <AddressButtons
            buttonText="Endorse"
            onButtonSubmit={() => {}}
            holderAddress={outgoingHolderAddress}
            beneficiaryAddress={outgoingBeneficiaryAddress}
          />
        </>
      );
    }}
  </Story>
</Preview>

---

### Errors

Block error state.

<Preview>
  <Story name="BlockErrored">
    {() => {
      const [outgoingHolderAddress, setOutgoingHolderAddress] = useState("");
      const [outgoingBeneficiaryAddress, setOutgoingBeneficiaryAddress] = useState("");
      return (
        <>
          <AddressInput
            address={outgoingHolderAddress}
            setAddress={setOutgoingHolderAddress}
            errorMessage="There's an error with holder address."
          />
          <AddressInput
            address={outgoingBeneficiaryAddress}
            setAddress={setOutgoingBeneficiaryAddress}
            errorMessage="There's an error with beneficiary address."
          />
          <AddressButtons
            buttonText="Endorse"
            onButtonSubmit={() => {}}
            holderAddress={outgoingHolderAddress}
            beneficiaryAddress={outgoingBeneficiaryAddress}
          />
        </>
      );
    }}
  </Story>
</Preview>

---

### Disabled

Disabled buttons below input block.

<Preview>
  <Story name="Disabled">
    <>
      <AddressInput address="0x28F7aB32C521D13F2E6980d072Ca7CA493020145" setAddress={() => {}} disabled />
      <AddressInput address="0x48399Fb88bcD031C556F53e9303CaF7aB32C9302" setAddress={() => {}} disabled />
      <AddressButtons
        buttonText="Confirm"
        onButtonSubmit={() => {}}
        holderAddress="0x28F7aB32C521D13F2E6980d072Ca7CA493020145"
        beneficiaryAddress="0x48399Fb88bcD031C556F53e9303CaF7aB32C9302"
        isDisabledAddressBook={true}
      />
    </>
  </Story>
</Preview>
