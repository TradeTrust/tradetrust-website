import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import {
  whenDocumentValidAndIssued,
  whenDocumentHashInvalid,
  whenDocumentRevoked,
  whenDocumentIssuerIdentityInvalid,
} from "../../../test/fixture/verifier-responses";
import CertificateVerifyCheck from "./CertificateVerifyCheck.js";

<Meta title="CertificateVerifyBlock/DetailedCertificateVerifyBlock" component={CertificateVerifyCheck} />

# Document is Valid

Document is issued, has integrity, not revoked and has a verified issuer identity

<Preview>
  <Story name="valid document">
    {() => {
      return <CertificateVerifyCheck verificationStatus={whenDocumentValidAndIssued} />;
    }}
  </Story>
</Preview>

---

# Document Hash is invalid

Document was tampered with

<Preview>
  <Story name="tampered document">
    {() => {
      return <CertificateVerifyCheck verificationStatus={whenDocumentHashInvalid} />;
    }}
  </Story>
</Preview>

# Document revoked

Document was issued and subsequently revoked

<Preview>
  <Story name="revoked document">
    {() => {
      return <CertificateVerifyCheck verificationStatus={whenDocumentRevoked} />;
    }}
  </Story>
</Preview>

# Document Issuer Identity invalid

Document's issuer identity cannot be verified

<Preview>
  <Story name="invalid issuer">
    {() => {
      return <CertificateVerifyCheck verificationStatus={whenDocumentIssuerIdentityInvalid} />;
    }}
  </Story>
</Preview>
